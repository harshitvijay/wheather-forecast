import { makeStyles } from "@mui/styles";

const useStyles = makeStyles({
  weather: {
    width: "80%",
    display: "flex",
    flexWrap: "wrap",
    justifyContent: "space-around",
    margin: "auto",
  },
});

export { useStyles };
