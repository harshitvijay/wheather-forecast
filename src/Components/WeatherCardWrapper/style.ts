import { makeStyles } from "@mui/styles";

const useStyles = makeStyles({
  weather: {
    display: "flex",
    justifyContent: "space-evenly",
    margin: "auto",
    marginTop: "-1.3rem",
    maxWidth: "80%",
  },
});

export { useStyles };
